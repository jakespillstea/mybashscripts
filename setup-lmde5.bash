# This is the bash script I will be using to configure LMDE5 at the office to be pure bliss and simplicity.
sudo apt update && sudo apt upgrade -y
# reboot
sudo apt install git htop vim chromium cheese pdfsam
flatpak install zoom pdfsam #onlyoffice
